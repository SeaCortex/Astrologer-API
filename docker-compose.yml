services:
    web:
        build: .
        ports:
            - '5001:5000'
        volumes:
            - .:/app
            - /app/.venv # Preserve container's venv, don't mount from host
        environment:
            - PORT=5000     - .env
